sudo: required
services:
  - docker

before_install:
  - source .env.sh
  - docker-compose build

language: python
python:
  - "3.7"
install:
  - pip install -r src/app/requirements.txt

script:
  - pytest tests
